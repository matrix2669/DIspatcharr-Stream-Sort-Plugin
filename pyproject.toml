[project]
name = "dispatcharr-stream-sort-plugin"
version = "0.2.1"
description = "Stream health analysis, scoring, and ordering for Dispatcharr"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
