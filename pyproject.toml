[project]
name = "dispatcharr-stream-sort-plugin"
version = "0.2.5"
description = "Stream health analysis, scoring, ordering, and runtime reliability telemetry for Dispatcharr"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
