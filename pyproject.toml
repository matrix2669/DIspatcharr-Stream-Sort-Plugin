[project]
name = "dispatcharr-stream-sort-plugin"
version = "0.1.0"
description = "Resolution-tiered stream ordering plugin for Dispatcharr"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
